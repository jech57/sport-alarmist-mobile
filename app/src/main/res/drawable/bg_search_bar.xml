<?xml version="1.0" encoding="utf-8"?>
<!-- "Filtrar por torneo..." search bar background (node 30:1839). Figma's
     node itself still shows a 28dp pill, but a review comment asked for the
     radius to match the design system's buttons/form fields (8dp, see
     node 14:5790 "Botón") instead. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_container" />
    <stroke
        android:width="1dp"
        android:color="@color/search_bar_border" />
    <corners android:radius="8dp" />
</shape>
