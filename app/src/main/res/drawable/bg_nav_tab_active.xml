<?xml version="1.0" encoding="utf-8"?>
<!--
  Selected tab pill (Figma node 30:1856, "Rectangle 2"): rgba(255,146,69,0.08)
  fill, 14dp radius. The border is a 3-stop vertical linear gradient
  (#FFC69C 95% -> #FF9245 75% at ~45% -> #7A3300 40%), which <stroke> inside a
  plain <shape> can't express (no gradient strokes), so it's faked with the
  usual two-shape trick: an outer rounded rect painted with the gradient
  (visible only at the edges) and an inner rounded rect, inset by the stroke
  width, painted with the real fill on top.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="14dp" />
            <gradient
                android:type="linear"
                android:angle="270"
                android:startColor="#F2FFC69C"
                android:centerColor="#BFFF9245"
                android:endColor="#667A3300" />
        </shape>
    </item>

    <item
        android:left="1.5dp"
        android:top="1.5dp"
        android:right="1.5dp"
        android:bottom="1.5dp">
        <shape android:shape="rectangle">
            <corners android:radius="12.5dp" />
            <solid android:color="@color/nav_tab_active_bg_opaque" />
        </shape>
    </item>

</layer-list>
