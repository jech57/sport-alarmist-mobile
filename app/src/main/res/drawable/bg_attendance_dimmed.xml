<?xml version="1.0" encoding="utf-8"?>
<!-- Botón no elegido cuando el otro está seleccionado. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" />
    <solid android:color="@color/attendance_dimmed_bg" />
    <stroke
        android:width="1dp"
        android:color="@color/attendance_dimmed_border" />
</shape>
